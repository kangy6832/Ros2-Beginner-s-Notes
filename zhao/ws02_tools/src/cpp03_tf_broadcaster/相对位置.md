TF:
    1.组成结构：广播方+监听方
    2.广播方负责发布相对关系数据
    3.监听方订阅多个广播方发布的数据，并将数据组织成坐标树，进而可实现任意坐标帧之间点或香向量的变换
    4.基于右手坐标系
    5.使用tf2实现
    注意：坐标变换时要参考时间戳！！！
    时间差在一定范围内

sudo apt-get install ros-humble-turtle-tf2-py ros-humble-tf2-tools
ros-humble-tf-transformations

sudo apt install python3-pip

pip3 install transforms3d

乌龟跟随：
ros2 launch turtle_tf2_py turtle_tf2_demo.launch.py //生成两只乌龟
ros2 run turtlesim turtle_teleop_key //控制

创建包:
     ros2 pkg create cpp03_tf_broadcaster --build-type ament_cmake --dependencies rclcpp tf2 tf2_ros geometry_msgs turtlesim
静态广播器的命令行实现：
    格式1.
    使用以米为单位的 x/y/z 偏移量和以弧度为单位的 roll/pitch/yaw（滚动/俯仰/偏航）
    ros2 run tf2_ros static_transform_publisher --x 0.4 --y 0 --z 0.2 ---yaw 0 --roll 0 --pitch 0 --frame-id base_link --child-frame-id laser
    或使用四元数（qx/qy/qz/qw）:
    ros2 run tf2_ros static_transform_publisher --x x --y y --z z --qx qx --qy qy --qz qz --qw qw --frame-id frame_id --child-frame-id child_frame_id